## Install Playwright Chromium

code2vision uses **Playwright** to launch a real Chromium browser, capture pixel-accurate screenshots, and intercept console logs — all without modifying your project.

Clicking the button above opens a terminal and runs:

```
npx playwright install chromium
```

This downloads ~170MB of browser binaries to your AppData folder. It only needs to be done once per machine.
